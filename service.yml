name: kafka-connect-http-demo
lang: java
lang_version: 8
git:
  enable: true
codeowners:
  enable: true
semaphore:
  enable: true
  pipeline_type: cp
  cve_scan: true
  extra_build_args: "-Dcloud -Pjenkins"
  extra_deploy_args: "-Dcloud -Pjenkins"
